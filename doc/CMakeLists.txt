#############################
# Global settings
#############################
CMAKE_MINIMUM_REQUIRED (VERSION 3.0)
PROJECT (docmarpaESLIF C CXX)

IF (UNIX)
  POD2MAN(Tutorial/Calculator/README.pod "MARPAESLIF_CALCULATOR" "3" "${MARPAESLIF_VERSION}")
  POD2MAN(BNF/README.pod "MARPAESLIF_BNF" "3" "${MARPAESLIF_VERSION}")
  POD2MAN(API/README.pod "MARPAESLIF_API" "3" "${MARPAESLIF_VERSION}")
ENDIF (UNIX)
